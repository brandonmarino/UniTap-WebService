require 'spec_helper'

describe Unitapapi do
  it 'has a version number' do
    expect(Unitapapi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
